import express from "express";
import { viewController } from "../../../controllers/view/viewController";
import { aboutController } from "../../../controllers/view/aboutController";
const router = express.Router();

// router.get("/home", viewController);
// router.get("/about", aboutController);

export default router;
